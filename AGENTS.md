# AGENTS.md — AutoPay 프로젝트 가이드

> 이 문서는 AutoPay 프로젝트의 목표, 아키텍처, 설계 원칙을 정의한다.
> 사람 개발자와 코딩 에이전트(Claude Code, Codex 등) 모두 이 문서를 프로젝트의
> 단일 기준(source of truth)으로 삼는다.

## 0. 포지셔닝 (가치 명제)

AutoPay의 가치 중심은 **"결제를 대신 실행하는 것"이 아니라 "에이전트가 돈을
쓸 때 사용자가 통제권과 가시성을 잃지 않게 하는 것"** 이다. 한 줄로:
**자율 지출 거버넌스 계층(Autonomous Spending Governance Layer).**

- 결제 실행(가장 fragile하고 플랫폼이 소유한 부분)에 사활을 걸지 않는다.
- **결제 수단에 독립적인** 정책 엔진 + 감사 + 통지 + 격리를 핵심 자산으로 한다.
- 네이버·카카오가 공식 agent-pay를 내더라도 **그 위에 얹는 안전 계층**으로
  생존하고, 미래 어떤 레일(빌링키/폰 승인/공식 API)이 와도 재사용된다.
- 즉 "누가 결제하느냐"가 아니라 **"누가 통제하고, 무엇이 기록되며, 언제
  알림이 가느냐"** 가 제품의 정체성이다.

## 1. 프로젝트 개요

AutoPay는 AI 에이전트가 사용자를 대신해 웹에서 상품을 검색하고 결제까지
수행하는 시스템이다. **주 타깃은 국내 결제 환경**(PG 결제창, 간편결제,
보안 키패드)이다. 핵심 가치는 다음 세 가지다.

1. **비밀정보 격리(Secret Isolation)** — 에이전트는 어떤 결제 비밀에도
   접근할 수 없다. 단, 이 격리는 "에이전트의 읽기를 차단"해서 달성하는 것이
   아니라(그건 대체로 불가능하다 — 2.6장) **"에이전트가 볼 수 있는 곳에
   훔칠 비밀 자체가 존재하지 않게"** 설계해서 달성한다.
2. **정책 기반 통제(Policy Enforcement)** — 결제 금액 한도, 결제 횟수,
   결제 수단, 구매 가능한 상품 카테고리, 허용 쇼핑몰 등을 사용자가
   정책으로 설정하고, 모든 결제는 정책 검증을 통과해야만 실행된다.
3. **즉시 통지(Notification & Audit)** — 결제가 완료되면 사용자에게 즉시
   알림을 보내고, 모든 결제 시도(성공/거절/실패)를 감사 로그로 남긴다.

### 범위와 전제 (Scope & Assumptions)

- **로그인은 범위 밖.** 아이디·비밀번호·2FA 등 로그인 자격증명은 AI에게
  절대 맡기지 않는다. AutoPay는 **사용자가 자기 브라우저에서 직접 로그인해
  둔, 이미 인증된 세션** 위에서만 동작한다. 에이전트도 브로커도 로그인
  자격증명을 저장·입력·취급하지 않는다.
- 따라서 브로커가 보관할 원시 결제 비밀(카드번호·CVV·결제 비밀번호)은
  기본 경로(패턴 B)에서 **존재하지 않는다.** 최종 인증은 결제사 앱(폰)이나
  빌링키(서버)가 처리하기 때문이다 (2.5장).

### 1.1 핵심 시나리오 (Flagship) — "지정가 자동 구매 + 원격 승인"

제품을 가장 설득력 있게 보여주는 대표 시나리오. **에이전트는 무엇을 살지
'판단'하지 않고, 사용자가 정한 조건의 '타이밍'만 실행한다** — 이것이 위험
인식을 최소화하는 핵심이다.

1. 사용자(익스텐션 UI): "이 노트북 스탠드, 3만원 밑 + 무료배송이면 사줘"
2. 에이전트: 지정 상품의 가격·재고 모니터링 (자율 판단 최소화 = 저위험)
3. 조건 충족 → 브로커가 정책 검증(한도·카테고리·머천트·횟수)
4. 브로커가 결제 진행 → 카카오/토스가 폰으로 승인 푸시 발송
   (결제사 푸시가 곧 승인 게이트 — 별도 승인 채널 불필요)
5. 사용자(폰): 지문/PIN으로 승인 → 결제 완료
6. 익스텐션 알림(`chrome.notifications`): "결제 완료 ₩28,900" + 감사 로그

**왜 설득되나:**
- **저위험**: AI가 상품을 고르지 않는다(사용자가 상품·상한가 지정) →
  "이상한 걸 살까봐"라는 최대 공포를 제거.
- **명백한 효용**: 가격·재고 기다림과 반복 체크의 귀찮음을 완전히 해소.
- **통제권 유지**: 결제 전 승인 + 정책 한도 + 즉시 통지 → 거버넌스 가치(0장)가
  기능으로 자연스럽게 드러남.
- **지금 구현 가능**: 패턴 B로 가맹점 계약 없이 오늘 만들 수 있음.

**인접 변형:**
- **완전 핸즈오프**: 소모품 재구매 + 사용자가 이미 등록한 자동결제(패턴 A) →
  정책 범위 내면 승인도 생략.
- **일회성 심부름**: "회의실용 HDMI 사줘" 단발 → 검색·비교·체크아웃 후 승인.

## 2. 신뢰 모델 (Trust Model)

이 프로젝트에서 가장 중요한 설계 결정은 **신뢰 경계(trust boundary)** 다.

```
┌─────────────────────────────┐        ┌──────────────────────────────┐
│  UNTRUSTED ZONE             │        │  TRUSTED ZONE                │
│                             │        │                              │
│  AI Agent                   │ 결제요청│  Payment Broker Extension    │
│  (Claude for Chrome, Codex, │───────▶│  ├─ Policy Engine            │
│   또는 이들이 쓰는 스킬/MCP) │◀───────│  ├─ Payment Executor         │
│                             │ 승인/거절│  ├─ Audit Log               │
│  - 상품 검색                 │        │  └─ Notifier                │
│  - 페이지 탐색/장바구니       │        │                              │
│  - 체크아웃 진입             │        │  - 정책 검증 후에만 결제 실행   │
│  - 웹페이지 콘텐츠에 노출됨   │        │  - out-of-band 최종 승인 요청  │
│    (프롬프트 인젝션 위험)     │        │  (원시 결제 비밀은 보관 안 함)  │
└─────────────────────────────┘        └──────────────────────────────┘
             │                                        │
             ▼                                        ▼
     [훔칠 비밀이 DOM에 없음]              [최종 승인: 폰 앱 / 빌링키]
```

- **에이전트는 신뢰하지 않는다.** 에이전트는 임의의 웹페이지 콘텐츠를
  읽으므로 프롬프트 인젝션에 노출된다. 에이전트가 완전히 탈취당해도
  비밀이 유출되지 않고, 정책을 벗어난 결제가 불가능해야 한다.
- **브로커가 에이전트에 제공하는 API에는 비밀을 반환하는 함수가 없다**
  (allowlist 방식의 최소 API). 이건 "안 주도록 검증"이 아니라 "줄 방법이
  없는" 구조다.
- **정책 엔진 + out-of-band 최종 승인이 마지막 방어선이다.** 에이전트가
  속아서 잘못된 결제를 요청해도, 정책(한도·횟수·머천트·카테고리)을 벗어나면
  거절되고, 통과하더라도 최종 승인은 에이전트가 닿을 수 없는 폰/서버에서
  일어난다.

> ⚠️ 격리의 실제 강제력은 통합 방식에 따라 다르다. 2.6장을 반드시 읽을 것.

## 2.5 결제 실행 모델 (조사 결과 기반, 필독)

`docs/payment-flows.md`의 국내 5개 결제수단(토스·카카오·네이버·쿠페이·삼성페이)
조사 결과, **국내 결제수단은 자동화된 결제 승인을 막도록 의도적으로 설계**되어
있다. 보안 키패드·별도 기기 앱 인증·생체인증은 우회 대상이 아니라 보안 기능이다.

따라서 이 프로젝트는 **"브로커가 결제 비밀번호/카드번호를 웹에 자동 입력한다"는
접근을 채택하지 않는다.** 그 접근은 대부분 기술적으로 불가능하고, 억지로
가능하게 만들려면 결제사 보안 통제를 무력화해야 하므로 약관 위반이자 사용자
계정 위험을 초래한다. 결제 자동화는 아래 두 가지 **정상 경로**로만 실현한다.

- **패턴 B — Human-in-the-loop 핸드오프 (⭐ 1차 목표, 제품의 기본값)**:
  에이전트가 검색~비교~장바구니~체크아웃 진입까지 수행하고, 최종 승인만
  사용자 폰(앱 인증/생체)으로 넘긴다. 완전 무인은 아니나 **어느 쇼핑몰에서나
  동작하는 범용성**을 얻고, 사용자 수고는 "지문 한 번"으로 축소되며 안전하다.
  삼성페이·앱카드·카카오·토스·네이버가 모두 여기 해당. 이것이 제품의
  핵심 UX다: "에이전트가 다 골라서 결제 직전까지 준비 → 사용자 폰 승인 한 번".
- **패턴 C — 사전 승인 무마찰 결제 (쿠팡 원터치)**: 사용자가 쿠팡 앱에서
  "원터치 결제"를 켜두면(최초 1회), 인-페이지에서 [결제하기] 클릭만으로
  결제가 완결된다(비번·폰 승인 없음). 기술적으로 가장 쉽지만(같은 origin,
  키패드·팝업 없음) **외부 인간 게이트가 없다.** 따라서 안전 책임이 전부
  **정책 엔진 + 우리 확인(confirm) + 즉시 통지**로 이동한다 — 이는 오히려
  본 프로젝트 원래 비전(정책 통제 + 결제 인지)에 부합한다. **빌링키·가맹점
  계약 없이 진짜 완전 핸즈오프가 되는 유일한 길**이라 지정가 감시 플래그십의
  완전 자동 변형에 적합. 제약: (1) 원터치(무비번) 경로만 사용 — 6자리 비번
  경로는 결제 비밀 저장/키패드 조작이 필요해 금지, (2) 원터치는 사용자가
  직접 켬, (3) 고액·비지정 구매는 우리 확인 게이트 강제, (4) 쿠팡 생태계 전용.
- **패턴 A — 빌링키/정기결제 (후순위, 무인 자동화 옵션)**: 최초 1회 사용자가
  앱에서 본인인증 → 빌링키(SID) 발급 → 이후 브로커가 정책 통제 하에
  서버-투-서버로 결제. 브로커는 원시 비밀을 만지지 않고 빌링키만 사용하므로
  비밀정보 격리(1장)에 완벽히 부합한다. 단 "지정 가맹점 반복 청구" 모델이라
  범용 일회성 결제에는 부적합하고 PG 가맹점 계약이 필요할 수 있어, **특정
  구독/정기배송 시나리오에 한해 후속 도입**한다.

**패턴 선택 기준**: 쿠팡 내 구매는 패턴 C(원터치)로 완전 자동(정책+확인+통지가
게이트). 쿠팡 외 범용 일회성 결제는 패턴 B(폰 승인)가 기본. 사용자가 특정
가맹점을 구독형으로 등록한 경우에 한해 패턴 A로 무인 처리. 정책 임계값 초과
시에는 어느 패턴이든 우리 확인(confirm)을 요구한다. 브로커는 비밀을 "자동
입력"하는 것이 아니라, 폰 승인을 "요청"하거나(B) 사전 승인된 원터치를
"클릭"하거나(C) 빌링키 결제를 "오케스트레이션"한다(A).

**채택하지 않는 것 (명시적 금지)**: 보안 키패드 우회, 합성 클릭 키패드 입력,
FDS/안티봇 회피, 카드번호·CVC 평문 보관/자동입력.

## 2.6 격리의 실제 강제력 (정직한 한계)

앞선 초안은 "에이전트가 결제창 DOM·쿠키·자동완성 필드를 못 읽게 막는다"고
서술했으나 **이는 대체로 사실이 아니며, 보안 모델을 여기에 의존해선 안 된다.**

**왜 못 막나.** Chrome의 익스텐션 격리는 한 익스텐션이 다른 익스텐션의 권한을
제한하지 못한다. 에이전트가 별도 익스텐션(예: Claude for Chrome)이라면 자기가
선언한 권한으로 동작하며, `cookies` 권한이 있으면 `chrome.cookies` API로
**HttpOnly 쿠키까지** 읽고(HttpOnly는 `document.cookie`만 막지 익스텐션 API는
못 막는다), host 권한이 있는 페이지는 DOM·스크린샷을 모두 취득한다. 브로커는
이를 막을 수단이 없다. "PG 도메인을 에이전트 사이트 접근에서 제외"하는 것도
**브로커가 강제하는 게 아니라 사용자가 Chrome 설정에서 그 에이전트의 사이트
접근을 직접 제한**해야만 성립한다 — 브로커는 확인만 할 수 있고 강제는 못 한다.

**그래서 진짜 방어선.**
1. **훔칠 게 없게 만든다.** 패턴 B/빌링키 설계상 에이전트가 보는 페이지 DOM에는
   카드번호·CVV·결제 비밀번호가 자동완성되지 않는다. "필드 읽기를 막는" 게
   아니라 **읽을 결제 비밀이 거기 없다.** 이것이 실제 격리의 핵심이다.
2. **탈취돼도 결제를 못 완료한다.** 정책 엔진 + out-of-band 최종 승인(폰/서버)을
   에이전트가 넘을 수 없다.

**통합 방식별 격리 수준 (중요).**

| 통합 방식 | 에이전트의 페이지 접근 | 격리 강제력 |
|---|---|---|
| **MCP/스킬로 브로커 호출** | 없음 — 브로커가 돌려주는 요약만 봄 | **강제 가능(우월)** |
| **별도 브라우저 익스텐션** (Claude for Chrome 등) | 자체 권한대로 DOM·쿠키 접근 | 사용자 설정 + "훔칠 게 없음"에 의존, 잔여 리스크 존재 |

→ 보안이 중요한 경로일수록 **MCP/스킬 통합을 우선**한다.

**잔여 리스크 (해결하지 않고 명시).** 에이전트 익스텐션이 쿠키/host 권한을
가지면 **이미 인증된 세션의 쿠키 탈취**를 브로커가 막을 수 없다. 이는 AutoPay가
만든 리스크가 아니라 **AI 에이전트 익스텐션을 브라우저에 설치하는 행위 자체에
내재된 리스크**이며, AutoPay의 통제 경계 밖이다. 사용자에게 이 전제를 고지하고,
민감 세션과 자동쇼핑 세션을 별도 브라우저 프로필로 분리할 것을 권고한다.

## 3. 아키텍처

> 시스템을 한 장으로 보는 통합 뷰(런타임·데이터흐름·상태·배포)는
> `docs/architecture.md`. 아래는 컴포넌트·API·플로우·정책 스키마의 요지.

### 3.1 컴포넌트

| 컴포넌트 | 역할 | 신뢰 수준 |
|---|---|---|
| **Agent Layer** | 상품 검색, 페이지 탐색, 장바구니 담기, 체크아웃 진입, 결제 요청 생성 | Untrusted |
| **Broker API** | 에이전트 ↔ 브로커 간 유일한 통신 채널. 구조화된 메시지 스키마만 허용 | 경계면 |
| **Policy Engine** | 결제 요청을 사용자 정책과 대조하여 승인/거절/사용자확인요구 판정 | Trusted |
| **Reference Store** | (패턴 A 도입 시) 빌링키·결제수단 참조를 암호화 저장. 원시 카드번호·CVV·비밀번호는 저장 안 함. 패턴 B 전용 단계에선 불필요 | Trusted |
| **Payment Executor** | 정책 통과 후 결제 수행. 패턴 B는 `SimplePayAdapter`(카카오/토스)가 결제창을 인수해 식별정보 입력→폰 푸시 트리거→완료 파싱. 패턴 A(빌링키)는 후순위 — 2.5장 참조 | Trusted |
| **Audit Log** | 모든 결제 시도의 불변 기록 (요청자, 금액, 머천트, 판정 결과, 시각) | Trusted |
| **Notifier** | 결제 완료/거절 시 사용자에게 즉시 알림 | Trusted |

### 3.2 에이전트 연동 방식 (두 가지 모두 지원 목표)

1. **Chrome Extension Messaging** — Claude for Chrome 같은 브라우저 에이전트가
   `chrome.runtime.sendMessage`(externally_connectable)로 브로커와 통신.
   브로커는 발신자 extension ID를 allowlist로 검증한다.
2. **MCP 서버 / 스킬** — Claude Code, Codex 등 로컬 에이전트를 위해 브로커가
   Native Messaging Host를 통해 로컬 MCP 서버를 노출. 에이전트는
   `search_status`, `request_payment`, `get_payment_result` 같은 도구만 사용.

두 경로 모두 동일한 Broker API 스키마와 동일한 정책 엔진을 거친다.

### 3.3 Broker API (에이전트에게 노출되는 전체 표면)

에이전트가 호출할 수 있는 것은 세 가지뿐이며, 비밀을 반환하는 API는 없다.

- `requestPayment(req) → { requestId }` — 결제 실행 "요청"만.
- `getPaymentResult(requestId) → PaymentResult` — 요약·사유만
  (approved / rejected / pending_user_confirmation / canceled / failed).
- `getPolicySummary() → { 잔여 예산·잔여 횟수·허용 카테고리/머천트/수단 }`.

> 정식 스키마(요청·응답 필드, confirm 해소, 오케스트레이션 순서)는
> `docs/spec/broker-api.md`, 타입은 `docs/spec/data-model.md`가 단일 진실이다.
> 이 절은 요지일 뿐 — 상충 시 스펙을 따른다.

### 3.4 결제 플로우

```
사용자: "5만원 이하로 무선 키보드 사줘"
  │
  ▼
[1] Agent: 상품 검색 → 비교 → 상품 선택 → 장바구니 → 체크아웃 페이지 진입
  │
  ▼
[2] Agent → Broker: requestPayment({merchant, items, totalAmount, ...})
  │
  ▼
[3] Policy Engine 검증:
      - 머천트 origin이 allowlist에 있는가?
      - totalAmount ≤ 건당 한도? 일/월 누적 한도 이내?
      - 오늘 결제 횟수 < 횟수 한도?
      - 상품 카테고리가 허용 목록에 있는가?
      - 결제 수단이 이 정책에서 허용되는가?
  │
  ├─ 위반 → rejected + 사유 반환, Audit Log 기록, (설정 시) 사용자 알림
  │
  ├─ 확인 필요(임계값 초과 등) → 브로커 UI로 사용자에게 승인 요청 (human-in-the-loop)
  │
  ▼ 통과
[4] Payment Executor:
      - 패턴 B: 브로커가 결제사 앱 인증(폰)을 트리거하고, 사용자에게
        "○○원, △△쇼핑몰 결제 승인해주세요" 알림 발송 → 사용자가 폰에서
        승인(생체/PIN) → 브로커가 체크아웃 완료 신호를 독립 파싱해 확인
      - 패턴 A: 빌링키로 서버-투-서버 결제 → 완료 확인
      (원시 결제 비밀은 브로커도 만지지 않는다 — 최종 인증은 폰/서버)
  │
  ▼
[5] Notifier: chrome.notifications + (옵션) 이메일/메신저로 즉시 통지
[6] Audit Log 기록 → Agent는 getPaymentResult()로 요약만 수신 → 사용자에게 보고
```

### 3.5 정책 스키마

정책은 다음으로 구성된다: **한도**(건당/일/월/일 횟수), **허용 머천트**
(allowlist | any), **카테고리**(allowlist | denylist), **결제수단**
(kakaopay/tosspay/coupay), **확인 규칙**(임계값 초과 시 확인·항상 확인),
**알림**(채널·거절 통지. MVP는 chrome만).

> 정식 Zod 스키마(`PaymentPolicy`)는 `docs/spec/data-model.md`가 단일 진실이다.
> 결제수단은 `shared`의 단일 `PaymentMethod` enum을 참조한다.

## 4. 비밀정보 격리 설계 원칙

격리는 "에이전트의 읽기를 차단"이 아니라 **"훔칠 비밀이 존재하지 않게" +
"최종 승인을 에이전트가 닿을 수 없는 곳(폰/서버)에 두기"** 로 달성한다
(근거와 한계는 2.6장).

1. **원시 결제 비밀을 브로커에 두지 않는다.** 기본 경로(패턴 B)에서 카드번호·
   CVV·결제 비밀번호는 저장하지도, 페이지에 입력하지도 않는다. 최종 인증은
   결제사 앱(폰)이 처리한다. → 에이전트가 볼 DOM에 결제 비밀이 없다.
2. **로그인 자격증명은 취급 안 함.** 아이디·비밀번호·2FA는 사용자가 직접
   로그인해 둔 세션을 사용할 뿐, 브로커/에이전트가 저장·입력하지 않는다.
3. **API 표면 최소화** — 브로커가 에이전트에 노출하는 API에 비밀·빌링키·
   세션정보를 반환하는 경로를 만들지 않는다. "안 주도록 검증"이 아니라
   "줄 방법이 없는" 구조.
4. **out-of-band 최종 승인** — 정책을 통과한 결제라도 최종 승인은 결제사 앱
   (폰 생체/PIN) 또는 빌링키 서버 호출에서 일어난다. 에이전트가 브라우저에서
   대리할 수 없는 채널이다. 이것이 "탈취된 에이전트도 결제를 완료 못 함"의
   근거다.
5. **참조 저장 시 암호화** — 패턴 A 도입 시 빌링키·결제수단 참조는
   `chrome.storage`에 평문 저장 금지. WebCrypto로 암호화하고, 키는 사용자
   패스프레이즈 또는 OS 키체인(Native Host 경유)에서 파생. (원시 카드번호는
   애초에 저장하지 않는다.)
6. **통합 방식은 MCP/스킬 우선** — 에이전트가 페이지 직접 접근 권한을 갖지
   않는 MCP/스킬 통합에서만 격리가 강제 가능하다. 별도 브라우저 익스텐션
   에이전트는 잔여 리스크(쿠키 탈취 등)를 사용자에게 고지한다 (2.6장).
7. **로그에 비밀 금지** — Audit Log, 콘솔, 에러 메시지 어디에도 비밀·빌링키
   원문/부분값을 남기지 않는다. 결제수단은 항상 `paymentMethodId`와
   label로만 참조.

## 5. 위협 모델 (요약)

| 위협 | 대응 |
|---|---|
| 악성 쇼핑몰 페이지의 프롬프트 인젝션으로 에이전트 탈취 | 에이전트는 비밀 접근 불가 + 정책 엔진 + out-of-band 최종 승인 |
| 에이전트가 결제창 DOM/화면에서 결제 비밀 읽기 | (읽기 차단이 아니라) 결제 비밀이 DOM에 애초에 없음 — 최종 인증은 폰/서버 (2.6장) |
| 에이전트가 금액을 속여서 결제 요청 | Executor가 체크아웃 페이지의 실제 결제 금액을 독립적으로 파싱·대조 |
| 승인~실행 사이 결제 대상 바꿔치기(TOCTOU) | 승인은 스냅샷에 바인딩, 실행 직전 재검증. 불일치 시 결제 중단(canceled) — `docs/spec/executor.md §2.1` |
| 허위 결제 완료 보고 | 브로커가 완료 신호를 독립 파싱 + 사용자 알림은 브로커가 직접 발송 |
| 다른 익스텐션이 브로커 API 호출 사칭 | 발신자 extension ID / Native Host 인증 검증 |
| 정책 우회를 위한 결제 분할 (한도 이하로 쪼개기) | 일/월 누적 한도 + 횟수 한도로 방어 |
| **[잔여] 에이전트 익스텐션의 세션 쿠키 탈취** | **브로커가 막을 수 없음.** 에이전트 설치 자체에 내재된 리스크로 사용자 고지 + 브라우저 프로필 분리 권고 (2.6장) |

## 6. 제안 디렉토리 구조

```
autopay/
├── AGENTS.md                  # 이 문서 (단일 진실)
├── CLAUDE.md                  # → AGENTS.md 심볼릭 링크
├── packages/
│   ├── broker-extension/      # 신뢰 영역: Chrome Extension (MV3)
│   │   ├── src/
│   │   │   ├── background/    # service worker: API 라우팅, 정책 엔진 호출
│   │   │   ├── policy/        # Policy Engine (순수 함수, 테스트 최우선)
│   │   │   ├── executor/      # Payment Executor (폰 승인 트리거/완료 파싱)
│   │   │   ├── notify/        # Notifier
│   │   │   ├── audit/         # Audit Log
│   │   │   ├── refstore/      # (패턴 A 도입 시) 빌링키 참조 암호화 저장
│   │   │   └── ui/            # 옵션 페이지(정책 관리), 승인 팝업
│   │   └── manifest.json
│   ├── shared/                # Broker API 메시지 스키마 (zod), 공용 타입
│   ├── mcp-server/            # 로컬 에이전트용 MCP 서버 (Native Host 경유)
│   └── agent-skill/           # Claude/Codex용 쇼핑 스킬 (프롬프트 + 도구 정의)
└── docs/
    ├── architecture.md        # 시스템 뷰(컴포넌트·데이터흐름·상태·배포)
    ├── payment-flows.md       # 결제수단 조사 + 패턴 B/C 플로우
    ├── stack.md               # 기술 스택 결정·근거
    ├── methodology.md         # 개발 방법론·DoD·워크플로
    ├── coding-guide.md        # 코딩 컨벤션·보안 코딩·테스트 규범
    ├── documentation-guide.md # 문서 품질 규칙·드리프트 방지·용어집
    └── spec/                  # 모듈별 스펙 (data-model, policy, broker-api,
                               #   executor, audit, notify, agent-integration)
```

## 7. 개발 원칙 (코딩 에이전트 포함 모든 기여자)

- **정책 엔진은 순수 함수로** — `(request, policy, usage, verifiedAmount) →
  decision`. 부수효과 없이 작성하고 유닛 테스트를 가장 두텁게 유지한다
  (`docs/spec/policy.md`).
- **신뢰 경계를 넘는 모든 메시지는 스키마 검증** — `packages/shared`의
  zod 스키마로 파싱 실패 시 즉시 거절.
- **비밀을 다루는 코드 변경은 반드시 4장 원칙과 대조** — refstore/executor
  수정 시 "이 변경으로 비밀·PII가 API·로그·DOM 밖으로 나갈 수 있는가"를
  PR 설명에 명시.
- **실제 카드 정보로 개발/테스트 금지** — 개발 중에는 더미값과 테스트
  머천트(스테이징 쇼핑몰, Stripe test mode 등)만 사용한다.
- **기본값은 보수적으로** — 새 정책 필드의 기본값은 항상 가장 제한적인
  값(deny)으로 한다.
- **스택·방법론·가이드는 별도 문서** — 스택 `docs/stack.md`, 방법론(스펙
  주도·TDD·DoD) `docs/methodology.md`, 코딩 규범 `docs/coding-guide.md`,
  문서 품질 규칙·용어집 `docs/documentation-guide.md`. 모듈 착수 전 해당
  `docs/spec/<module>.md`를 먼저 작성한다(없으면 코드보다 스펙 우선).

## 8. 마일스톤 (초안)

1. **M0 — 스켈레톤**: 모노레포 셋업, shared 스키마, 정책 엔진 코어 + 테스트
2. **M1 — 브로커 익스텐션**: Side Panel(활동·승인) + Options(정책 UI) +
   chrome.notifications. shadcn neutral·각진 4px·Geist·light/dark (`docs/spec/ui.md`,
   `docs/design/ui-mockup.html`). 원시 비밀 저장 없음(로그인/결제비밀 범위 밖)
3. **M2 — 에이전트 연동**: MVP는 **두뇌 내장(①)** + Broker API 도구
   인터페이스, 더미 결제 E2E(테스트 쇼핑몰). 격리 강화용 **MCP/스킬(③)** 은
   동일 인터페이스로 후속(목표), 브라우저-익스텐션 메시징도 후속
   (`docs/spec/agent-integration.md`).
4. **M3 — Payment Executor (카카오 → 쿠팡 → 토스)**: `SimplePayAdapter` 정의
   후 **카카오(패턴 B) 최초 검증** → **쿠팡(패턴 C, 원터치)** → **토스(패턴 B)**.
   - 패턴 B: 결제창 인수(휴대폰/생년월일 입력) → 폰 푸시 → 폰 승인 대기.
   - 패턴 C: [결제하기] 클릭(원터치) → 외부 게이트 없어 confirm 게이트+통지.
   공통: `verify()`로 금액 독립 검증, 완료 신호 독립 파싱.
5. **M4 — 강화**: 패턴 A(빌링키) 구독 시나리오 도입, 임계값 기반 A/B 분기,
   감사 로그 뷰어, (선택) 원격 알림·명령 채널(텔레그램 등)

## 9. 미결정 사항 (Open Questions)

- [ ] (별도 익스텐션 에이전트 사용 시) 에이전트의 사이트 접근 설정을
      브로커가 검증·안내할 수 있는 범위 (chrome.management 등) — 강제는
      불가하므로 사용자 셋업 가이드 + 검증 경고 수준
- [ ] 패턴 B 핸드오프 UX (1차 목표의 핵심) — 에이전트가 체크아웃 진입 후
      사용자 폰 승인까지 대기하는 흐름의 추적/타임아웃/취소 UX 상세.
      (승인 대기 중 대상 바꿔치기 방어는 해결됨 — 스냅샷 재검증,
      `docs/spec/executor.md §2.1`.)
- [ ] 패턴 B에서 브로커가 "결제 완료"를 어떻게 독립 확인하나 — 에이전트
      보고가 아니라 체크아웃 페이지의 완료 신호/주문번호를 브로커가 직접 파싱
- [ ] 첫 타깃 쇼핑몰·결제수단 조합 선정 — 패턴 B가 가장 매끄럽게 동작하는
      곳부터 (완료 신호 파싱이 쉬운 쇼핑몰 우선)
- [ ] (후순위) 패턴 A 빌링키 — 개인 개발자가 빌링키 API를 쓸 수 있는지 vs
      PG 가맹점 계약 필요 여부 (구독 시나리오 도입 시점에 재검토)
- [ ] 결제창 도메인 화이트리스트를 실결제 네트워크 캡처로 확정
      (docs/payment-flows.md의 미확정 도메인들)
- [ ] Native Messaging Host 배포 방식 (설치 스크립트 vs 앱 번들)
